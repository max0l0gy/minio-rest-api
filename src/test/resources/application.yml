spring:
  application:
    name: minio-rest-api
  profiles:
    active: test
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

minio:
  endpoint: http://localhost:9000
  accessKey: minio_access_key
  secretKey: minio_secret_key

file:
  endpoint: http://file.titsonfire.store
  accessKey: minio_rest_api_accessKey

server:
  port: 8080
